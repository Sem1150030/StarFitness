# Cursor Rules for StarFitness Project
  When making modals always use the livewire component with ModalComponent extension. every modal must begin with a div: {<div class="bg-slate-800 p-6 rounded-lg shadow-2xl">} so the UI is consistent


